# TrailWatch mobile

Flutter visitor application for scanning a ticket QR code, joining a park trip, sharing GPS updates, sending an SOS, and completing the trip.

Run against the local TrailWatch server:

```bash
flutter run
```

The app connects to `https://dash.dilanp.duckdns.org` by default. The server URL can also be changed from the app's join screen.
